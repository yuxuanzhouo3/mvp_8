# ============================================
# 环境变量示例文件 (可以推送到Git)
# ============================================
# 使用方法: 复制此文件为 .env.local 并填写真实值

# ============================================
# 部署区域配置 (核心配置)
# ============================================
# 值: 'china' | 'overseas'
# 默认: china（国内版，不填此变量即为国内版）
# china: 使用CloudBase数据库 + 微信/支付宝支付（默认）
# overseas: 使用Supabase数据库 + Stripe/PayPal支付（海外部署时设置）
NEXT_PUBLIC_DEPLOYMENT_REGION=overseas


# Supabase配置 (海外部署使用)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
# 海外安卓套壳 Google 原生登录（用于 signInWithIdToken）
NATIVE_GOOGLE_WEB_CLIENT_ID=your_google_web_client_id.apps.googleusercontent.com

# 腾讯云CloudBase配置 (国内部署使用)
CLOUDBASE_ENV=your_cloudbase_env_id
NEXT_PUBLIC_CLOUDBASE_ENV=your_cloudbase_env_id
NEXT_PUBLIC_WECHAT_CLOUDBASE_ID=your_cloudbase_env_id

# CloudBase密钥 (服务端专用)
CLOUDBASE_SECRET_ID=your_cloudbase_secret_id
CLOUDBASE_SECRET_KEY=your_cloudbase_secret_key

# 国内版邮箱验证码 SMTP 配置（注册/找回密码）
AUTH_EMAIL_SMTP_HOST=smtp.example.com
AUTH_EMAIL_SMTP_PORT=465
AUTH_EMAIL_SMTP_USER=your_smtp_user
AUTH_EMAIL_SMTP_PASS=your_smtp_password
AUTH_EMAIL_FROM=your_from_email

# 网站URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000
