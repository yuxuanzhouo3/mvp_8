# 🧪 前端邮箱认证测试指南

## 📍 如何打开测试页面

### 1. 确认开发服务器正在运行

开发服务器应该已经在运行。如果没有，运行：
```bash
npm run dev
```

### 2. 打开浏览器

访问：**http://localhost:3000**

---

## 🎯 如何打开登录/注册模态框

### 方法 1：点击登录按钮

1. 在页面顶部或右上角找到 **"登录"** 或 **"Login"** 按钮
2. 点击该按钮
3. 登录模态框会弹出

### 方法 2：通过编程方式打开

如果你知道在哪里可以触发打开模态框的功能，也可以通过代码来控制。

---

## 🔍 如何查看测试结果

### 打开浏览器开发者工具

1. **按快捷键**：
   - Windows/Linux: `F12` 或 `Ctrl + Shift + I`
   - Mac: `Cmd + Option + I`

2. **或者右键点击页面**：
   - 选择 **"检查"** 或 **"Inspect"**

### 查看控制台日志

1. 点击开发者工具顶部的 **"Console"**（控制台）标签
2. 在这个标签页中，你会看到所有的 `console.log` 和 `console.error` 输出

---

## 📝 测试步骤

### 测试注册功能

1. **打开登录模态框**
2. **切换到注册模式**（如果默认是登录模式，点击"注册"或"Sign Up"切换）
3. **填写邮箱**：例如 `test@example.com`
4. **填写密码**：例如 `test123456`（至少6位）
5. **点击提交按钮**
6. **查看控制台**：应该看到 `认证成功: { success: true, message: "注册成功", user: {...} }`

### 测试登录功能

1. **打开登录模态框**
2. **确保是登录模式**（如果没有用户，先用注册功能创建一个）
3. **填写邮箱**：使用之前注册的邮箱
4. **填写密码**：使用之前注册的密码
5. **点击提交按钮**
6. **查看控制台**：应该看到 `认证成功: { success: true, message: "登录成功", user: {...} }`

---

## 🎨 预期结果

### 注册成功

控制台应该显示：
```javascript
认证成功: {
  success: true,
  message: "注册成功",
  user: {
    id: "4798591468fca29700ac2fc14a7cab7b",
    email: "test@example.com",
    name: "test",
    pro: false,
    region: "china"
  }
}
```

### 登录成功

控制台应该显示：
```javascript
认证成功: {
  success: true,
  message: "登录成功",
  user: {
    id: "4798591468fca29700ac2fc14a7cab7b",
    email: "test@example.com",
    name: "test",
    pro: false,
    region: "china"
  }
}
```

### 认证失败

控制台应该显示：
```javascript
认证失败: Error: [错误信息]
```

---

## ⚠️ 常见问题

### Q: 找不到登录按钮？
A: 查看页面顶部导航栏或右下角，通常有 "登录" 或 "Login" 按钮。

### Q: 模态框没有弹出？
A: 检查浏览器控制台是否有 JavaScript 错误。

### Q: 看不到控制台输出？
A: 确保在开发者工具的 "Console" 标签页中查看。

### Q: 显示网络错误？
A: 确保开发服务器正在运行，访问 http://localhost:3000

---

## 🎉 测试完成后

如果看到 "认证成功" 的控制台输出，说明：
- ✅ API 调用成功
- ✅ 数据正确传递
- ✅ 后端处理正常

接下来可以进行下一步：处理返回的用户数据并集成到应用中。


