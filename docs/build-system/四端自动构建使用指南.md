# 🚀 四端自动构建使用指南

## 📋 概述

已配置 GitHub Actions 自动构建系统,可以一键生成:
- ✅ Android APK
- ✅ Mac DMG
- ✅ Windows MSI
- ⏳ iOS IPA (需要手动构建,因为需要Apple证书)

---

## 🎯 使用方法

### 方法1: 自动触发(推荐)

每次修改 `desktop-apps` 文件夹后:
```bash
git add .
git commit -m "你的提交信息"
git push origin main
```

**GitHub会自动开始构建**,10-20分钟后生成所有安装包。

---

### 方法2: 手动触发

1. 访问您的GitHub仓库
2. 点击顶部的 **Actions** 标签
3. 在左侧选择 **🚀 四端自动构建**
4. 点击右侧的 **Run workflow** 按钮
5. 选择 **main** 分支
6. 点击绿色的 **Run workflow** 按钮

---

## 📥 下载安装包

构建完成后(约15-20分钟):

1. 访问 **GitHub仓库 → Actions**
2. 点击最新的工作流运行记录
3. 滚动到底部,找到 **Artifacts** 区域
4. 下载以下文件:

```
📦 SiteHub-AllPlatforms-Release.zip  (所有平台打包在一起)
   ├── SiteHub-Android/
   │   └── app-release.apk
   ├── SiteHub-Mac/
   │   └── SiteHub_1.0.0_x64.dmg
   └── SiteHub-Windows/
       └── SiteHub_1.0.0_x64_en-US.msi
```

---

## 🔧 构建前准备(首次使用)

### Android 项目准备

Android项目需要Gradle Wrapper,执行以下命令初始化:

```bash
cd desktop-apps/android-twa

# 如果没有gradlew,需要创建
# 方法1: 使用Android Studio打开项目,会自动生成
# 方法2: 手动下载gradle wrapper
gradle wrapper
```

**检查清单**:
- [ ] `gradlew` 文件存在
- [ ] `gradle/wrapper/gradle-wrapper.jar` 存在
- [ ] `gradle/wrapper/gradle-wrapper.properties` 存在

---

## 📱 iOS 手动构建(需要Mac + Xcode)

因为iOS需要Apple开发者证书,无法自动构建。请按以下步骤手动构建:

### 步骤:

```bash
cd desktop-apps/ios-capacitor

# 1. 安装依赖
npm install

# 2. 同步Capacitor配置
npx cap sync ios

# 3. 打开Xcode项目
npx cap open ios
```

### 在Xcode中:
1. 连接iPhone或选择模拟器
2. 设置 **Signing & Capabilities**
3. 选择您的开发者证书
4. **Product → Archive**
5. **Distribute App** → 选择发布方式:
   - **App Store Connect** (上架App Store)
   - **Ad Hoc** (内测分发)
   - **Development** (开发测试)

---

## 🐛 常见问题

### Q1: GitHub Actions构建失败

**检查**:
1. 访问 **Actions** 页面查看错误日志
2. 常见原因:
   - Android缺少gradle wrapper
   - Tauri配置文件错误
   - 磁盘空间不足

**解决**:
```bash
# 本地测试构建
cd desktop-apps/mac-tauri
cargo tauri build
```

---

### Q2: 下载的安装包无法运行

**Mac应用**:
```bash
# 如果提示"无法打开,因为来自身份不明的开发者"
xattr -cr SiteHub.app

# 或者在系统偏好设置中允许:
# 系统偏好设置 → 安全性与隐私 → 通用 → 仍要打开
```

**Windows应用**:
- 右键 → 属性 → 解除锁定
- 或添加数字签名(生产环境需要)

**Android应用**:
- 需要启用"允许安装未知来源的应用"
- 或使用签名密钥签名APK

---

### Q3: 想添加应用图标

每个平台的图标文件:
```
desktop-apps/
├── android-twa/app/src/main/res/
│   ├── mipmap-hdpi/ic_launcher.png (72x72)
│   ├── mipmap-mdpi/ic_launcher.png (48x48)
│   ├── mipmap-xhdpi/ic_launcher.png (96x96)
│   ├── mipmap-xxhdpi/ic_launcher.png (144x144)
│   └── mipmap-xxxhdpi/ic_launcher.png (192x192)
│
├── ios-capacitor/ios/App/Assets.xcassets/AppIcon.appiconset/
│   └── (多个尺寸的PNG)
│
├── mac-tauri/src-tauri/icons/
│   ├── icon.icns (Mac)
│   └── icon.png (1024x1024)
│
└── windows-tauri/src-tauri/icons/
    ├── icon.ico (Windows)
    └── icon.png (1024x1024)
```

---

## 📊 构建状态监控

### 实时查看构建进度:

1. GitHub仓库 → **Actions** 标签
2. 点击正在运行的工作流
3. 查看每个job的实时日志:
   - 📱 构建 Android APK
   - 🍎 构建 Mac DMG
   - 🪟 构建 Windows MSI
   - 📦 打包所有安装包

### 成功标志:
```
✅ build-android
✅ build-mac
✅ build-windows
✅ package-all
```

---

## 🎁 交付给Jeff

构建完成后,将以下内容打包:

```
SiteHub-Release-Package/
├── 📱 Android/
│   └── SiteHub.apk
├── 🍎 Mac/
│   └── SiteHub.dmg
├── 🪟 Windows/
│   └── SiteHub.msi
├── 📱 iOS/
│   └── (手动构建后的IPA)
└── 📄 README.md (安装说明)
```

---

## 🔄 版本更新流程

网站更新后:**无需重新构建**

因为所有应用都是加载 `https://www.mornhub.help`,所以:
- ✅ 网站更新 → 所有端立即生效
- ✅ 用户无需重新下载应用
- ✅ 真正的"一次构建,永久使用"

**只在以下情况需要重新构建**:
- 更改应用名称
- 更改应用图标
- 更改目标URL
- 升级系统版本支持

---

## 📞 技术支持

如有问题,检查:
1. **GitHub Actions日志**: 查看具体构建错误
2. **SPEC文档**: `文档归档/SPEC_多端显化执行手册.md`
3. **项目README**: `desktop-apps/README.md`

---

**✨ 现在,您可以一键生成所有安装包了!**

祝构建顺利! 🚀
