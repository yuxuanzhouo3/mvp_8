# 部署检查清单

## 📋 部署前必做事项

### ✅ 代码改动检查

已修改的文件：
- ✅ `.env.local` - 环境变量配置
- ✅ `app/api/auth/wechat/callback/route.ts` - 微信登录回调API
- ✅ `components/auth-modal.tsx` - 登录界面（添加微信登录按钮）
- ✅ `app/page.tsx` - 主页面（添加微信登录回调处理）

新增的文件：
- ✅ `微信登录配置步骤.md` - 配置文档
- ✅ `微信登录功能实现完成.md` - 实现报告
- ✅ `CLAUDE.md` - 项目架构文档
- ✅ `部署检查清单.md` - 本文档

---

## 🔐 Vercel 环境变量配置

### 必须配置的环境变量（共11个）

#### 1. 微信登录配置（2个）
```
WECHAT_APP_ID=wxcf9a932db9c53cfe
WECHAT_APP_SECRET=62499fac74e065b37c45e651bc22eefa
```
⚠️ 设置为 **Sensitive** （仅Owner可见）

#### 2. 腾讯云CloudBase配置（3个）
```
NEXT_PUBLIC_WECHAT_CLOUDBASE_ID=你的云开发环境ID
CLOUDBASE_SECRET_ID=你的腾讯云SecretId
CLOUDBASE_SECRET_KEY=你的腾讯云SecretKey
```
⚠️ `CLOUDBASE_SECRET_ID` 和 `CLOUDBASE_SECRET_KEY` 设置为 **Sensitive**

#### 3. JWT密钥（1个）
```
JWT_SECRET=mornhub-production-jwt-secret-2025-change-this-in-production
```
⚠️ 设置为 **Sensitive**
⚠️ **重要：** 生产环境请使用不同的密钥！

#### 4. 网站URL（1个）
```
NEXT_PUBLIC_SITE_URL=https://mornhub.help
```

#### 5. Supabase配置（2个 - 海外用户备用）
```
NEXT_PUBLIC_SUPABASE_URL=https://ykirhilnbvsanqyenusf.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlraXJoaWxuYnZzYW5xeWVudXNmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTI5NzA5OTQsImV4cCI6MjA2ODU0Njk5NH0.AHf66dC0vqu43WFET1zzosMMKIWwcvPlDIKCnVQli0Y
```

#### 6. 支付配置（2个 - 已有）
```
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=（已配置）
STRIPE_SECRET_KEY=（已配置）
```

---

## 📝 Vercel 环境变量配置步骤

### 方法1：通过Vercel Dashboard配置

1. **登录Vercel**
   - 访问：https://vercel.com/
   - 找到您的项目

2. **进入设置**
   - 点击项目名称
   - 点击 **Settings** 标签
   - 左侧菜单选择 **Environment Variables**

3. **添加环境变量**
   对于每个变量：
   - 点击 **Add New**
   - **Key**: 填写变量名（如 `WECHAT_APP_ID`）
   - **Value**: 填写变量值
   - **Environments**: 勾选 `Production`, `Preview`, `Development`
   - 如果是敏感信息，勾选 **Sensitive**
   - 点击 **Save**

4. **重新部署**
   配置完所有变量后，需要重新部署项目才能生效

---

### 方法2：通过命令行配置（推荐，更快）

创建一个临时文件 `.env.production`：

```bash
# 微信登录
WECHAT_APP_ID=wxcf9a932db9c53cfe
WECHAT_APP_SECRET=62499fac74e065b37c45e651bc22eefa

# 腾讯云CloudBase
NEXT_PUBLIC_WECHAT_CLOUDBASE_ID=你的云开发环境ID
CLOUDBASE_SECRET_ID=你的腾讯云SecretId
CLOUDBASE_SECRET_KEY=你的腾讯云SecretKey

# JWT密钥（⚠️ 生产环境请改为更安全的密钥）
JWT_SECRET=mornhub-production-jwt-secret-2025-change-this-in-production

# 网站URL
NEXT_PUBLIC_SITE_URL=https://mornhub.help

# Supabase（海外用户）
NEXT_PUBLIC_SUPABASE_URL=https://ykirhilnbvsanqyenusf.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlraXJoaWxuYnZzYW5xeWVudXNmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTI5NzA5OTQsImV4cCI6MjA2ODU0Njk5NH0.AHf66dC0vqu43WFET1zzosMMKIWwcvPlDIKCnVQli0Y
```

然后使用Vercel CLI批量导入：
```bash
vercel env pull .env.production
```

⚠️ **配置完后删除这个文件！不要提交到Git！**

---

## 🔍 部署前检查

### 1. Git状态检查
```bash
git status
```

应该看到：
- ✅ 已修改的文件列表
- ⚠️ 确保 `.env.local` **不在列表中**（已被.gitignore忽略）

### 2. 敏感信息检查
确保以下文件**不会**被提交：
- ❌ `.env.local`
- ❌ `.env.production`
- ❌ 任何包含密钥的文件

### 3. 构建测试
```bash
npm run build
```

确保构建成功，无错误。

---

## 🚢 部署步骤

### 选项A：通过Git推送自动部署（推荐）

1. **提交代码**
   ```bash
   git add .
   git commit -m "feat: 添加国内用户微信登录功能

   - 添加微信登录回调API，生成JWT Token
   - 在登录界面添加微信登录按钮（国内用户优先）
   - 支持web_users集合的loginType字段
   - 添加微信登录回调处理逻辑
   - 保持邮箱登录功能不受影响

   🤖 Generated with Claude Code
   Co-Authored-By: Claude <noreply@anthropic.com>"
   ```

2. **推送到远程仓库**
   ```bash
   git push origin feature/cn-email-login
   ```

3. **Vercel会自动检测并部署**
   - 访问 Vercel Dashboard 查看部署进度
   - 等待部署完成（通常2-5分钟）

### 选项B：通过Vercel CLI部署

```bash
vercel --prod
```

---

## ✅ 部署后验证清单

### 1. 访问网站
- 访问：https://mornhub.help
- 检查页面是否正常加载

### 2. 检查登录界面
1. 点击右上角"登录"按钮
2. **国内用户应该看到：**
   - ✅ 绿色"微信登录"按钮（顶部）
   - ✅ 分隔线"或使用邮箱登录"
   - ✅ 邮箱登录表单（下方）
3. **海外用户应该看到：**
   - ✅ "Google登录"按钮（顶部）
   - ✅ 分隔线"or continue with"
   - ✅ 邮箱登录表单（下方）

### 3. 测试邮箱登录（不依赖微信授权）
1. 使用邮箱和密码登录
2. **验证：**
   - ✅ 能正常登录
   - ✅ 右上角显示用户信息
   - ✅ 刷新页面后登录状态保持

### 4. 查看浏览器控制台
- 打开 F12 开发者工具
- 查看 Console 标签
- **不应该有严重错误**（红色）

### 5. 检查环境变量
1. 在控制台输入：
   ```javascript
   console.log(process.env.NEXT_PUBLIC_SITE_URL)
   ```
2. **应该显示：** `https://mornhub.help`

---

## 🔴 微信登录测试（需要授权回调域）

⚠️ **前置条件：必须先填写微信授权回调域**

### 1. 填写授权回调域
1. 登录：https://open.weixin.qq.com/
2. 进入：管理中心 → 网站应用 → 您的应用
3. 填写授权回调域：`mornhub.help`（不要带https://）
4. 提交审核（个人开发者通常立即通过）

### 2. 测试微信登录
1. 访问 https://mornhub.help
2. 点击"登录" → 点击"微信登录"
3. 使用微信扫码授权
4. **验证：**
   - ✅ 跳转回首页
   - ✅ 用户已登录
   - ✅ 右上角显示微信昵称
   - ✅ localStorage包含 `user_token` 和 `user_info`

### 3. 查看数据库
1. 登录腾讯云：https://console.cloud.tencent.com/tcb
2. 进入环境：`你的云开发环境ID`
3. 查看 `web_users` 集合
4. **验证：** 新增的微信用户记录包含：
   - ✅ `_openid`
   - ✅ `nickname`
   - ✅ `avatar`
   - ✅ `loginType: 'wechat'`

---

## ⚠️ 常见问题

### Q1: 部署后环境变量不生效
**解决：**
- 确保在Vercel Dashboard中配置了所有环境变量
- 重新部署项目
- 清除浏览器缓存

### Q2: 微信登录报错 "redirect_uri参数错误"
**解决：**
- 检查微信开放平台的授权回调域是否为 `mornhub.help`
- 检查 `NEXT_PUBLIC_SITE_URL` 是否为 `https://mornhub.help`

### Q3: 邮箱登录失败
**解决：**
- 检查 `CLOUDBASE_SECRET_ID` 和 `CLOUDBASE_SECRET_KEY` 是否正确
- 检查 `NEXT_PUBLIC_WECHAT_CLOUDBASE_ID` 是否正确
- 查看Vercel部署日志

### Q4: JWT Token错误
**解决：**
- 检查 `JWT_SECRET` 是否已配置
- 确保生产环境使用独立的JWT密钥

---

## 📊 部署完成后的下一步

### ✅ 微信登录功能验证完成后
1. 测试邮箱登录和微信登录都正常工作
2. 验证数据保存到CloudBase数据库
3. 确认登录状态可以保持7天

### 🚀 准备四端转化
完成验证后，可以开始：
1. **小程序端** - 微信小程序适配
2. **APP端** - React Native / Flutter 适配
3. **H5端** - 移动端H5适配
4. **PC端** - 桌面端优化

---

## 📝 总结

**部署前必做：**
- [ ] 检查Git状态，确保不提交敏感文件
- [ ] 在Vercel配置11个环境变量
- [ ] 运行 `npm run build` 测试构建
- [ ] 提交代码并推送

**部署后必做：**
- [ ] 验证网站可以访问
- [ ] 测试邮箱登录功能
- [ ] 填写微信授权回调域
- [ ] 测试微信登录功能
- [ ] 查看CloudBase数据库记录

**一切正常后：**
- [ ] 开始四端转化工作 🚀

---

**创建时间：** 2025-10-28
**版本：** v1.0
