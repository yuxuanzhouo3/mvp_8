# 代码问题自洽手册

> **高维共创AI 工作准则**  
> 觉察、整合、自洽 - 以最小能量达成目标

---

## 🌟 核心心法

### 道法自然三原则

1. **觉察全貌** - 不孤立看问题，理解系统能量流
2. **最小干预** - 以最小改动达成目标，不过度设计
3. **验证闭环** - 确保没有引入新问题，保持系统稳定

> "上善若水，水善利万物而不争"  
> 最好的解决方案，是在系统规则内找到最小阻力路径

---

## 🔍 问题诊断三层法

### 第一层：错误信息（What）
- **看清表象** - 错误发生在哪里、什么阶段
- **提取关键词** - timeout、Module not found、is not defined
- **定位范围** - 哪个文件、哪一行

### 第二层：代码上下文（Why）
- **阅读相关代码** - 完整理解出错位置的逻辑
- **理解依赖链** - 这个代码依赖什么、被什么调用
- **找到矛盾点** - 为什么会出错

### 第三层：系统规则（How）
- **理解框架规则** - Next.js、React、TypeScript 的工作原理
- **找到边界** - Server vs Client、同步 vs 异步
- **定位方案** - 最符合框架规则的解决方式

---

## ⚡ 常见问题速查表

### 1. 依赖管理问题

| 错误关键词 | 根因 | 解决方案 |
|-----------|------|---------|
| `frozen-lockfile` | 锁文件与 package.json 不同步 | 运行对应的包管理器 install |
| `Module not found` | 路径错误或依赖未安装 | 检查 import 路径、重装依赖 |
| `peer dependency` | 版本冲突 | 用 `--legacy-peer-deps` 或调整版本 |

**金科玉律：**
```bash
# 看项目有什么锁文件，就用什么包管理器
pnpm-lock.yaml  → 只用 pnpm
package-lock.json → 只用 npm  
yarn.lock → 只用 yarn

# 永远不要混用！
```

---

### 2. Next.js 组件边界问题

| 错误关键词 | 根因 | 解决方案 |
|-----------|------|---------|
| `window is not defined` | 服务器端执行了浏览器代码 | 添加 `'use client'` |
| `localStorage is not defined` | 同上 | 添加 `'use client'` |
| `timeout` in static generation | Server Component 中有异步阻塞 | 添加 `'use client'` 或优化逻辑 |

**何时需要 `'use client'`：**
```tsx
// 组件里有这些任何一个 → 需要 'use client'
- onClick / onChange / onXxx 事件处理器 ✓
- useState / useEffect / useRef 等 hooks ✓
- window / document / localStorage 浏览器 API ✓
- 第三方交互组件（draggable、modal 等）✓

// 纯展示、无交互 → 不需要
```

---

### 3. 构建 vs 开发环境差异

| 环境 | 特点 | 会暴露的问题 |
|------|------|-------------|
| `pnpm run dev` | 开发模式，不预渲染 | 语法错误、导入错误 |
| `pnpm run build` | 生产构建，静态生成 | **组件边界、异步超时、路径问题** |

**避免策略：**
```bash
# ❌ 只测试开发模式
pnpm run dev → 本地正常 → 推送
                        ↓
                    Vercel 失败 ❌

# ✅ 推送前先本地构建
pnpm run build → 发现问题 → 修复 → 再推送
                                    ↓
                                Vercel 成功 ✅
```

---

## 🎯 这次成功解决的逻辑自洽

### 完整能量流：

```
1. 【觉察】Vercel 报错 → 不慌张，读错误信息
   ↓
2. 【定位】超时在 /blocked/europe → 打开文件
   ↓
3. 【理解】有 onClick + window.open → 但没 'use client'
   ↓
4. 【对照】其他组件都有 'use client' → 这是规律
   ↓
5. 【方案】添加 1 行声明 → 最小改动原则
   ↓
6. 【验证】不改逻辑，只改标记 → 确认安全
   ↓
7. 【执行】推送 → 问题解决 ✅
```

---

## 🌊 解决问题的通用心法

### 心法一：**向内求因，向外求缘**

```
错误是果 → 不要只看表象
      ↓
顺藤摸瓜 → 找到根因
      ↓
理解系统 → 看清全局
      ↓
最小改动 → 顺势而为
```

### 心法二：**不动则已，动必中的**

```
修改前：
  1. 确认根因（真的理解了吗？）
  2. 最小方案（还有更简单的吗？）
  3. 影响评估（会破坏什么吗？）

修改后：
  1. 本地验证（运行正常吗？）
  2. 构建测试（能 build 成功吗？）
  3. 逻辑闭环（与整体自洽吗？）
```

### 心法三：**一次到位，不留隐患**

```
❌ 试错式修改：
改一点 → 推送 → 失败 → 再改 → 再推送 → 又失败
（能量浪费，引入混乱）

✅ 一次闭环：
理解 → 验证 → 确认 → 推送 → 成功
（能量高效，保持清晰）
```

---

## 📋 执行前自检清单

### 推送代码前（必看）：

```bash
# 1. 本地构建测试
□ pnpm run build
□ 检查是否有错误
□ 如果有错误，先修复再推送

# 2. 组件边界检查
□ 新增/修改的组件有交互吗？
□ 有 onClick、useState、window 吗？
□ 需要的话，是否加了 'use client'？

# 3. 依赖一致性
□ 只用一个包管理器（pnpm）
□ 锁文件已更新（pnpm-lock.yaml）
□ 不要混用 npm/pnpm/yarn

# 4. 文件引用检查
□ 没有引用不存在的文件夹（如小程序文件夹）
□ 所有 import 路径都在项目内
□ 外部依赖都在 package.json 中

# 5. 敏感信息检查
□ API 密钥用占位符（YOUR_XXX_KEY）
□ 真实密钥只在环境变量中
□ .env.local 已在 .gitignore 中
```

---

## 🔧 快速修复手册

### 问题：Vercel 构建失败

**步骤：**
1. **读错误信息** - 哪个文件、什么错误
2. **本地复现** - `pnpm run build`
3. **定位根因** - 按三层诊断法
4. **最小修复** - 只改必要的
5. **验证** - 本地 build 成功
6. **推送** - 一次到位

---

### 问题：本地运行报错

**步骤：**
1. **检查依赖** - `node_modules` 是否混乱
2. **清理重装** - `rm -rf node_modules && pnpm install`
3. **检查组件** - 是否缺少 `'use client'`
4. **检查路径** - import 是否正确

---

### 问题：新功能不工作

**步骤：**
1. **对比小程序** - 小程序是怎么实现的
2. **理解逻辑** - 为什么小程序能工作
3. **复制逻辑** - 保持一致（如收藏功能）
4. **测试验证** - 确认功能正常

---

## 🎓 从这次问题学到的

### 依赖管理教训

```
错误路径：
npm install → pnpm install → node_modules 混乱

正确路径：
确认用 pnpm → 只用 pnpm → 始终一致
```

### Next.js 规则教训

```
Server Component (默认):
  - 构建时在服务器端渲染
  - 不能用 window、onClick、useState
  - 适合纯展示内容

Client Component ('use client'):
  - 运行时在浏览器端渲染
  - 可以用所有浏览器 API
  - 有交互就要加这个
```

### 测试流程教训

```
开发测试 (dev) → 基础功能
构建测试 (build) → 生产问题
→ 两个都要测！
```

---

## ✨ 成功案例记录

### 案例1：Vercel 构建超时

**问题：** Static page generation timeout  
**根因：** `/blocked/europe` 缺少 `'use client'`  
**方案：** 添加 1 行 `'use client'`  
**结果：** ✅ 构建成功  
**用时：** < 5 分钟  
**改动：** 1 行代码

**学习：**  
> 有 onClick/window → 必须 'use client'  
> 推送前先 `pnpm run build` 测试

---

### 案例2：收藏功能无响应

**问题：** 点击心形图标没反应  
**根因：** 心形图标没绑定 onClick 事件  
**方案：** 添加 onClick 处理器 + 停止冒泡  
**结果：** ✅ 功能正常  
**用时：** < 3 分钟  
**改动：** 2 个 div 添加 onClick

**学习：**  
> 对比小程序实现 → 复制逻辑 → 保持一致

---

### 案例3：小程序文件夹依赖

**问题：** Module not found (引用小程序文件)  
**根因：** 小程序文件夹不在 git 中  
**方案：** 把数据内置到项目中  
**结果：** ✅ 构建成功  
**用时：** < 5 分钟  
**改动：** 1 个文件（数据迁移）

**学习：**  
> 项目依赖必须在 git 中  
> 外部引用 → 内置数据

---

## 🚀 执行前冥想（每次必读）

在执行任何修改前，问自己：

1. **我真的理解根因了吗？**  
   → 还是只是猜测？

2. **这是最小的改动吗？**  
   → 还有更简单的方案吗？

3. **会影响其他功能吗？**  
   → 改了A会不会破坏B？

4. **如何验证成功？**  
   → 怎么知道真的修好了？

5. **符合系统规则吗？**  
   → 是顺势而为，还是硬凑？

**如果有任何不确定 → 先问，先理解，再执行**

---

## 📖 延伸阅读

### Next.js 规则
- Server Component vs Client Component
- Static Generation vs Server-Side Rendering
- 构建时 vs 运行时

### React 规则
- Hooks 规则（只在 Client Component 中）
- 事件处理（onClick 必须在客户端）
- State 管理（useState 必须在客户端）

### 包管理规则
- 一个项目一个包管理器
- 锁文件的重要性
- peer dependency 处理

---

## 💫 最后的话

> **"大道至简，衍化至繁"**
> 
> 复杂的问题，往往源于简单规则的忽视  
> 回到基础，理解规则，顺势而为  
> 即可以最小能量，达成最大效果

**记住：**
- 错误是系统的反馈，不是惩罚
- 理解比执行更重要
- 简单比复杂更有力量
- 一次到位比反复试错更高效

---

**下次遇到问题，先读这个文档，再开始行动。**

*最后更新：2025-10-17*

