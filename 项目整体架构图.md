# 🌳 **MVP8项目整体架构图 - 一眼看懂**

## 🎯 **项目概述**

**MVP8是什么？** 一个智能网站导航平台，帮助用户收藏和管理常用网站

**核心功能：** 
- 网站收藏和管理
- 用户注册登录
- 支付订阅
- 智能推荐

---

## 🏗️ **整体架构（俯瞰图）**

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户访问层                                │
├─────────────────────────────────────────────────────────────────┤
│  中国用户(IP:CN)        │        海外用户(IP:海外)              │
│  mornhub.help          │        mornhub.help                   │
└─────────┬───────────────┴─────────┬─────────────────────────────┘
          │                         │
          ▼                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DNS智能解析层                               │
├─────────────────────────────────────────────────────────────────┤
│  境内解析 → 腾讯云     │    境外解析 → Vercel                    │
└─────────┬───────────────┴─────────┬─────────────────────────────┘
          │                         │
          ▼                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      应用部署层                                  │
├─────────────────────────────────────────────────────────────────┤
│  腾讯云Serverless      │        Vercel部署                      │
│  site.mornscience.top  │        mornhub.help                    │
└─────────┬───────────────┴─────────┬─────────────────────────────┘
          │                         │
          ▼                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      数据存储层                                  │
├─────────────────────────────────────────────────────────────────┤
│  腾讯云云开发数据库    │        Supabase数据库                  │
│  (境内合规)           │        (海外快速)                       │
└─────────┬───────────────┴─────────┬─────────────────────────────┘
          │                         │
          ▼                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      第三方服务层                                │
├─────────────────────────────────────────────────────────────────┤
│  微信支付 + 支付宝     │        Stripe + PayPal + 支付宝         │
│  微信登录 + 邮箱       │        Google登录 + 邮箱                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔧 **前端架构详解**

### **技术栈：**
```
Next.js 14.2.16 (React框架)
├── TypeScript (类型安全)
├── Tailwind CSS (样式)
├── Shadcn/ui (组件库)
├── Dnd-kit (拖拽功能)
└── React Hooks (状态管理)
```

### **页面结构：**
```
app/
├── page.tsx              # 首页（网站收藏列表）
├── auth/
│   ├── login/            # 登录页面
│   └── signup/           # 注册页面
├── payment/              # 支付页面
├── dashboard/            # 用户仪表板
├── settings/             # 设置页面
└── api/                  # API路由
```

### **组件结构：**
```
components/
├── ui/                   # 基础UI组件
│   ├── button.tsx
│   ├── modal.tsx
│   └── ...
├── auth-modal.tsx        # 登录弹窗
├── payment-modal.tsx     # 支付弹窗
├── site-grid.tsx         # 网站网格
├── website-card.tsx      # 网站卡片
└── ...
```

---

## 🔧 **后端架构详解**

### **API路由结构：**
```
app/api/
├── auth/
│   ├── callback/         # OAuth回调
│   └── wechat/callback/  # 微信登录回调
├── payment/
│   ├── stripe/           # Stripe支付
│   ├── paypal/           # PayPal支付
│   ├── alipay/           # 支付宝支付
│   └── wechat/           # 微信支付
│       ├── create/       # 创建订单
│       └── notify/       # 支付回调
└── sites/                # 网站管理API
```

### **数据库适配器模式：**
```
lib/database/
├── adapter.ts            # 数据库适配器接口
├── supabase-adapter.ts   # Supabase实现
├── cloudbase-adapter.ts  # 腾讯云实现
└── cloudbase-client.ts   # 腾讯云客户端
```

---

## 💾 **数据库架构**

### **腾讯云数据库（境内用户）：**
```
云开发数据库集合：
├── web_favorites          # 用户收藏
├── web_custom_sites       # 自定义网站
├── web_subscriptions      # 订阅信息
├── web_payment_transactions # 支付记录
└── web_user_profiles      # 用户资料
```

### **Supabase数据库（海外用户）：**
```
PostgreSQL表：
├── web_favorites          # 用户收藏
├── web_custom_sites       # 自定义网站
├── web_subscriptions      # 订阅信息
├── web_payment_transactions # 支付记录
└── web_user_profiles      # 用户资料
```

---

## 🔐 **环境变量详解**

### **数据库配置：**
```bash
# Supabase（海外用户）
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 腾讯云（境内用户）
NEXT_PUBLIC_WECHAT_CLOUDBASE_ID=cloud1-xxx
```

### **支付配置：**
```bash
# Stripe（海外用户）
STRIPE_PUBLIC_KEY=pk_test_xxx
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# PayPal（海外用户）
PAYPAL_CLIENT_ID=xxx
PAYPAL_CLIENT_SECRET=xxx

# 支付宝（全球用户）
NEXT_PUBLIC_ALIPAY_APP_ID=xxx
ALIPAY_PRIVATE_KEY=xxx

# 微信支付（境内用户）
WECHAT_PAY_APP_ID=wx_xxx
WECHAT_PAY_MCH_ID=xxx
WECHAT_PAY_SERIAL_NO=xxx
WECHAT_PAY_PRIVATE_KEY=xxx
WECHAT_PAY_PUBLIC_KEY=xxx
WECHAT_PAY_API_V3_KEY=xxx
```

### **登录配置：**
```bash
# Google登录（海外用户）
GOOGLE_CLIENT_ID=xxx
GOOGLE_CLIENT_SECRET=xxx

# 微信登录（境内用户）
WECHAT_APP_ID=wx_xxx
WECHAT_APP_SECRET=xxx
```

---

## 🌍 **智能分流逻辑**

### **用户访问流程：**
```
1. 用户访问 mornhub.help
   ↓
2. DNS智能解析
   ├── 中国IP → 腾讯云Serverless
   └── 海外IP → Vercel
   ↓
3. 应用检测用户IP
   ├── isChina=true → 使用腾讯云数据库
   └── isChina=false → 使用Supabase数据库
   ↓
4. 显示对应UI
   ├── 中国用户 → 微信支付 + 微信登录
   └── 海外用户 → Stripe/PayPal + Google登录
```

### **数据库选择逻辑：**
```typescript
// 在 app/page.tsx 中
const dbAdapter = user.type === "authenticated" && user.id
  ? createDatabaseAdapter(isChina, user.id)  // 根据IP选择数据库
  : null

// 在 lib/database/adapter.ts 中
export function createDatabaseAdapter(isChina: boolean, userId: string) {
  if (isChina) {
    return new CloudBaseAdapter(userId)  // 腾讯云
  } else {
    return new SupabaseAdapter(userId)   // Supabase
  }
}
```

---

## 💰 **支付流程架构**

### **境内用户支付流程：**
```
用户点击支付
   ↓
选择微信支付/支付宝
   ↓
调用 /api/payment/wechat/create
   ↓
返回二维码/支付链接
   ↓
用户完成支付
   ↓
微信/支付宝回调 /api/payment/wechat/notify
   ↓
更新腾讯云数据库
   ↓
激活用户订阅
```

### **海外用户支付流程：**
```
用户点击支付
   ↓
选择Stripe/PayPal/支付宝
   ↓
调用对应API
   ↓
跳转支付页面
   ↓
用户完成支付
   ↓
支付平台回调
   ↓
更新Supabase数据库
   ↓
激活用户订阅
```

---

## 🔑 **认证流程架构**

### **境内用户登录：**
```
用户点击登录
   ↓
显示微信登录 + 邮箱登录
   ↓
微信登录 → 微信OAuth → /api/auth/wechat/callback
   ↓
创建/更新腾讯云用户记录
   ↓
返回JWT Token
```

### **海外用户登录：**
```
用户点击登录
   ↓
显示Google登录 + 邮箱登录
   ↓
Google登录 → Google OAuth → /api/auth/callback
   ↓
创建/更新Supabase用户记录
   ↓
返回JWT Token
```

---

## 📱 **前端状态管理**

### **Context结构：**
```
contexts/
├── auth-context.tsx      # 用户认证状态
├── geo-context.tsx       # 地理位置状态
├── language-context.tsx  # 语言状态
└── settings-context.tsx  # 设置状态
```

### **Hook结构：**
```
hooks/
├── use-custom-websites.ts # 自定义网站管理
├── use-favorites.ts       # 收藏管理
├── use-mobile.tsx         # 移动端检测
├── use-toast.ts           # 消息提示
└── use-wechat-auth.ts     # 微信认证
```

---

## 🚀 **部署架构**

### **Vercel部署（海外）：**
```
GitHub仓库
   ↓
Vercel自动部署
   ↓
全球CDN分发
   ↓
海外用户快速访问
```

### **腾讯云部署（境内）：**
```
GitHub仓库
   ↓
腾讯云Serverless
   ↓
境内CDN分发
   ↓
境内用户快速访问
```

---

## 🔄 **数据同步机制**

### **用户数据迁移：**
```
新用户注册
   ↓
检测IP位置
   ├── 境内 → 腾讯云数据库
   └── 海外 → Supabase数据库
   ↓
localStorage数据迁移到云端
   ↓
清除本地存储
```

### **年度数据备份：**
```
年度报告生成
   ↓
导出腾讯云数据
   ↓
导出Supabase数据
   ↓
合并统计报告
   ↓
备份到U盘
```

---

## 🎯 **核心文件说明**

### **关键配置文件：**
```
next.config.mjs           # Next.js配置
tailwind.config.ts        # 样式配置
components.json           # 组件库配置
tsconfig.json            # TypeScript配置
```

### **环境配置：**
```
env.example              # 环境变量示例
.env.local               # 本地环境变量
.env.production          # 生产环境变量
```

### **数据库脚本：**
```
scripts/
├── create-web-tables-fixed.sql  # Supabase表创建
└── 腾讯云集合创建指南.md        # 腾讯云集合创建
```

---

## 📊 **监控和日志**

### **错误监控：**
```
前端错误 → 用户反馈
API错误 → 服务器日志
数据库错误 → 数据库日志
支付错误 → 支付平台日志
```

### **性能监控：**
```
页面加载时间
API响应时间
数据库查询时间
支付成功率
用户转化率
```

---

## 🎉 **总结**

**MVP8是一个：**
- ✅ 智能分流的网站导航平台
- ✅ 双数据库架构确保数据合规
- ✅ 本地化支付和登录体验
- ✅ 全球用户快速访问
- ✅ 完整的用户生命周期管理

**核心技术特点：**
- 🌍 全球部署 + 智能分流
- 💾 双数据库适配器模式
- 💰 多支付方式本地化
- 🔐 多登录方式本地化
- 📱 响应式设计 + 拖拽功能

---

*这就是MVP8的完整架构！就像看一片树叶，现在你能一眼看出它的结构了！* 🍃
